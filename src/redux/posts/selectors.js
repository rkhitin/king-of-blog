export const postsSelector = state => state.posts.items

export const loadingSelector = state => state.posts.isLoading

export const errorMessageSelector = state => state.posts.errorMessage

export const reeditedItemSelector = state => state.posts.reeditedItem
